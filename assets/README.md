# Assets

Diagrams and images referenced from the docs.

Keep source files (`.drawio`, `.excalidraw`) alongside exports (`.svg`, `.png`)
so diagrams stay editable.

Do not add product UI screenshots — they date within weeks and become
misleading faster than they become useful.
